{
    "framework": "nextjs",
    "buildCommand": "prisma generate && next build",
    "installCommand": "npm ci --legacy-peer-deps",
    "outputDirectory": ".next",
    "regions": ["iad1"],
    "functions": {
          "app/api/**": {
                  "maxDuration": 30
          }
    }
}
